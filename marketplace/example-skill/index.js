// Example community skill. Pure, inert, and side-effect free: it does not touch
// the network, the filesystem, or the shell, so it scans clean and serves as a
// template for new submissions.
export function run() {
  return 'Hello from the example skill!';
}
