# Channels

Channels are modular adapters with manifests, config schema, auth setup, receive/send adapters, health checks, reconnect strategy, rate limits, and permission policy. WebChat/local and CLI are implemented starters. Slack, WhatsApp, Discord, Email, and Teams are placeholders until safe adapters are wired with pairing and allowlists.
