# Plugin SDK

Plugins declare a manifest, capabilities, permissions, config schema, secrets schema, approval policy, lifecycle hooks, runtime context, logger, audit interface, and health hook. Use `defineHelmrPlugin` and contract-test plugins before installing them into a production Gateway.
