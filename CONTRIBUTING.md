# Contributing

Run `npm ci`, `npm run typecheck`, `npm test`, and `npm run test:contracts` before submitting changes. New externally visible capabilities need docs, tests, audit events, approval policy, and rollback/disable guidance.
