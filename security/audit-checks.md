# Audit Checks

`helmr security audit` checks API token, production mode, wildcard CORS, public bind, state/secrets permissions, dangerous provider config, channel exposure, tool permissions, write approvals, budgets, audit log, and backups.
